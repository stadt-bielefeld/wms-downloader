{
  "name": "wms-downloader",
  "version": "0.0.1",
  "description": "Allows download of web map services (WMS) as tiles.",
  "main": "index.js",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Jens Pabel <jens.pabel@bielefeld.de>",
  "license": "MIT",
  "dependencies": {
    "gm": "^1.21.1",
    "request": "^2.69.0"
  }
}
